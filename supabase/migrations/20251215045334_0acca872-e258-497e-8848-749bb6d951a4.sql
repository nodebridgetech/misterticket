-- Add new status comment for documentation
-- The withdrawal_requests.status field now supports: pending, awaiting_transfer, completed, rejected, failed

-- No migration needed - status is a text field that already supports any value